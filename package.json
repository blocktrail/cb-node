{
  "name": "cb-node",
  "version": "1.0.0",
  "description": "common blockchain wrapper for common-blockchain-node-server",
  "main": "./src/index.js",
  "scripts": {
    "coverage": "istanbul cover _mocha --timeout 10000 -- test/*.js",
    "coveralls": "npm run coverage && coveralls < coverage/lcov.info",
    "test": "istanbul test mocha --timeout 10000 -- --reporter list test/*.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dcousens/cb-node.git"
  },
  "author": "Daniel Cousens",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/dcousens/cb-node/issues"
  },
  "homepage": "https://github.com/dcousens/cb-node",
  "devDependencies": {
    "cb-tester": "^0.1.x",
    "coveralls": "^2.11.2",
    "istanbul": "^0.3.5",
    "mocha": "^2.1.0"
  },
  "dependencies": {
    "httpify": "^1.0.0",
    "jsend": "^0.1.2"
  }
}
